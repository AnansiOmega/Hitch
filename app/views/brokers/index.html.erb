<h1>Please select an option below</h1>

<p><%= button_to "Sign up", new_broker_path, method: :get %></p>
<p><%= button_to "Login", broker_login_path, method: :get %></p>



  <% @brokers.each do |broker| %>
    <p><%= link_to broker.name, broker_path(broker) %></p>
  <% end %>
